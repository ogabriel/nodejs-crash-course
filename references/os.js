const os = require('os');

// Platform
console.log(os.platform());

// CPU Arch
console.log(os.arch());

// CPU Core Info
console.log(os.cpus());

// Total memory
console.log(os.totalmem());

// Free memory
console.log(os.freemem());
